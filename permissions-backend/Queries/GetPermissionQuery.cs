/**
 * The GetPermissionQuery is a simple class that contains a single property, Id, which is the identifier of the permission we want to retrieve.
 */
public class GetPermissionQuery
{
    public Guid Id { get; set; }
}